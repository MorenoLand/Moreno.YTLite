{
  "name": "ytlite-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:dev": "tsc && vite build --minify false --mode development",
    "build": "tsc && vite build --mode production",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": { "@wailsio/runtime": "3.0.0-beta.11" },
  "devDependencies": { "typescript": "^5.7.3", "vite": "^8.2.0" }
}
