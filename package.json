{
  "name": "tauritube",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "app": "tauri dev",
    "frontend:build": "tsc && vite build",
    "build": "tauri build",
    "build:local": "tauri build --config src-tauri/tauri.local.conf.json",
    "tauri:build": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": { "@tauri-apps/api": "^2" },
  "devDependencies": { "@tauri-apps/cli": "^2", "typescript": "~5.6.2", "vite": "^6.0.3" }
}
